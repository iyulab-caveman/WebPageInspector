<?xml version="1.0" encoding="utf-8"?>
<iyu:NavigationPage
    x:Class="WebPageInspector.Pages.WebScraperPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebPageInspector.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:iyu="using:Iyu.Windows.Navigation"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Border Style="{StaticResource Border.Page}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Style="{StaticResource Border.Title}">
                <TextBlock Text="Web Scraper" Style="{StaticResource TextBlock.PageTitle}" />
            </Border>

            <Border Grid.Row="1" Style="{StaticResource Border.Content}">
                <ScrollViewer>
                    <StackPanel Style="{StaticResource StackPanel.PageContent}">
                        <Border Style="{StaticResource Border.Card}" MinHeight="120">
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</iyu:NavigationPage>